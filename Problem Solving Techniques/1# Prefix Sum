#include <bits/stdc++.h>
#include <algorithm>

using namespace std;

#define Line "\n"

/******** Prefix Sum ***********/


int main() {

/*
* 1# prefix sum in 1D array

//long long arr[1000002];
//long long p[1000002] = {0}; // Initialize p[0] to 0
   int n, q;
    cin >> n >> q;
    for (int i = 1; i <= n; i++) {
        cin >> arr[i];
    }
    for (int i = 1; i <= n; i++) { // prefix sum start from 1 not zero
        p[i] = arr[i] + p[i - 1];   // Update prefix sum array correctly
    }
    while (q--) {
        long long L, R;
        cin >> L >> R;
        long long result = p[R] - p[L - 1];
        cout << result << Line;
    }
*/

/* using prefix sum to solve new problem  ->> to update range   */ 
 /*
 long long arr[100002];
long long prefixSumarr[100002]={0};
int n,q;
cin >> n >> q;
for(int i =1;i<=n;i++){ // better to start from 1 to avoid error
    cin >>arr[i];
}while(q--){
   int L,R,val;
   cin >> L >> R >> val;
   prefixSumarr[L]+=val;
   prefixSumarr[R+1]-=val;
}for(int i =1;i<=n;i++){
   prefixSumarr[i]+=prefixSumarr[i-1];
}for(int i =1;i<=n;i++){
cout << prefixSumarr[i]+arr[i]<<" ";
} cout << endl;

    return 0;
*/
}
